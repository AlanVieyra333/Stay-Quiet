<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_action_arrow</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="element">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="divider" parent="@style/element">
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/colorGray</item>
        <item name="android:layout_weight">1</item>
        <item name="android:divider">?android:dividerHorizontal</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:showDividers">middle</item>
    </style>

    <style name="textNormal" parent="@style/element">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="textHight" parent="@style/element">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="textLow" parent="@style/element">
        <item name="android:textSize">12sp</item>
    </style>

    <style name="editText">
        <item name="android:background">@drawable/layout_border</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:drawableRight">@android:drawable/ic_menu_close_clear_cancel</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>
